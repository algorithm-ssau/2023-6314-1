spring:
  profiles:
    active: dev

  application:
    name: order-service

  cloud:
    config:
      uri: http://config-service:8888
      fail-fast: true
      username: admin
      password: admin
